name: pokemon
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  #packages
  flutter_screenutil: ^5.6.0
  google_fonts: ^3.0.1
  lottie: ^2.1.0
  flutter_bloc: ^8.1.1
  dio: ^4.0.6
  get_it: ^7.2.0
  shimmer: ^2.0.0
  flutter_rating_bar: ^4.0.1

  #design system
  poke_design_system:
    git:
      url: https://github.com/kindaniel/pokeds.git
      ref: main  


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  bloc_test: ^9.1.0
  mocktail: ^0.3.0
  test_cov_console: ^0.2.2

flutter:

  uses-material-design: true

  assets:
  - assets/images/
  - assets/lottie/

